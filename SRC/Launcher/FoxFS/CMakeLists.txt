file(GLOB_RECURSE FOXFS_HEADER CONFIGURE_DEPENDS "*.h")
file(GLOB_RECURSE FOXFS_SOURCE CONFIGURE_DEPENDS "*.cpp")

add_library(FoxFS STATIC ${FOXFS_HEADER} ${FOXFS_SOURCE})

target_link_libraries(FoxFS
	PUBLIC F_FS
)