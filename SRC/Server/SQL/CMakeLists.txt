file(GLOB_RECURSE SERVER_SQL_HEADER CONFIGURE_DEPENDS "*.h")
file(GLOB_RECURSE SERVER_SQL_SOURCE CONFIGURE_DEPENDS "*.cpp")

add_library(S_SQL STATIC ${SERVER_SQL_HEADER} ${SERVER_SQL_SOURCE})

target_link_libraries(S_SQL PUBLIC SERVER)
