file(GLOB_RECURSE SERVER_BASE_HEADER CONFIGURE_DEPENDS "*.h")
file(GLOB_RECURSE SERVER_BASE_SOURCE CONFIGURE_DEPENDS "*.cpp")

add_library(S_Base STATIC ${SERVER_BASE_HEADER} ${SERVER_BASE_SOURCE})

target_link_libraries(S_Base PUBLIC SERVER)