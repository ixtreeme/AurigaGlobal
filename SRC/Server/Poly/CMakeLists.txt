file(GLOB_RECURSE SERVER_POLY_HEADER CONFIGURE_DEPENDS "*.h")
file(GLOB_RECURSE SERVER_POLY_SOURCE CONFIGURE_DEPENDS "*.cpp")

add_library(S_Poly STATIC ${SERVER_POLY_HEADER} ${SERVER_POLY_SOURCE})

target_link_libraries(S_Poly PUBLIC SERVER)

