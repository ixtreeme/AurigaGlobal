Phase 15B-1 — GayaSystem entity-first pilot

Date: 2026-04-19
Build gate: PASS

Scope
- File: SRC/Server/GameServer/ecs/systems/GayaSystem.cpp
- Helper: SRC/Server/GameServer/ecs/AIHelpers.hpp

Step 1 audit summary
- GayaSystem public API was already entity-based in GayaSystem.hpp.
- Internal implementation still used a local helper that resolved entity -> VIDComponent -> CHARACTER_MANAGER::Find(...).
- Internal state authority is still legacy CHARACTER-owned:
  - load_gaya_items
  - load_gaya_values
  - info_items
  - info_slots
  - GayaUpdateTime
  - GetGaya / PointChange(POINT_GAYA)
- No direct external callers to GayaSystem::* were found outside the file-local CHARACTER wrappers.
- External gameplay callers still use CHARACTER wrappers such as:
  - CraftGayaItems
  - MarketGayaItems
  - RefreshGayaItems
  - UpdateItemsGayaMarker
  - InfoGayaMarker

Step 2 caller audit
- Direct external GayaSystem::* callers: 0
- Caller cascade risk: low
- Existing wrapper model:
  - CHARACTER::* methods bridge to GayaSystem::* via AIHelpers::EcsOf(this)

Step 3 signature decisions
- Load: OPTION_ENTITY (already entity)
- CheckItemsFull: OPTION_ENTITY
- ClearMarket: OPTION_ENTITY
- InfoMarket: OPTION_ENTITY
- CheckSlot: OPTION_ENTITY
- BuyItems: OPTION_ENTITY
- RefreshItemsMarket: OPTION_ENTITY
- UpdateSlot: OPTION_ENTITY
- UpdateItems0: OPTION_ENTITY
- UpdateItems: OPTION_ENTITY
- CraftItems: OPTION_ENTITY
- MarketItems: OPTION_ENTITY
- RefreshItems: OPTION_ENTITY
- GetState: OPTION_ENTITY
- SetState: OPTION_ENTITY
- StartCheckTimeMarket: OPTION_ENTITY

No KEEP_LEGACY_OVERLOAD was needed because the wrapper layer already existed on CHARACTER.

Implementation pattern summary
- Pattern A (read from ECS component instead of CHARACTER): 0
  - Reason: no dedicated ECS Gaya component / market-state component exists yet.
  - Gaya data coverage remains a Phase 15D component-expansion concern.

- Pattern B (legacy mutating authority through one resolved CHARACTER bridge): 8
  - ClearMarket
  - BuyItems
  - UpdateSlot
  - UpdateItems0
  - UpdateItems
  - CraftItems
  - RefreshItems
  - SetState

- Pattern C (legacy packet/chat/event interaction through one resolved CHARACTER bridge): 6
  - check_time_market_event
  - InfoMarket
  - BuyItems
  - CraftItems
  - MarketItems
  - RefreshItems

- Pattern D (resolve legacy pointer once at top of entity-entry function): 16
  - Every GayaSystem entity-entry function now begins with a single:
    auto* ch = LegacyCharOf(entity);
  - The old repeated entity -> VIDComponent -> Find(...) pattern is gone.

Concrete changes
- Replaced local entity -> CHARACTER resolution from:
  - VIDComponent + CHARACTER_MANAGER::Find(...)
  to:
  - LegacyCharPtr component lookup
- Removed dependency on char_manager.cpp lookup from GayaSystem internals.
- Added AIHelpers::EcsOf(const CHARACTER*) overload to remove const_cast noise from the const wrapper path.

Counts
- Baseline LPCHARACTER count from migration audit: 17
- Post-migration raw LPCHARACTER / CHARACTER* token count in GayaSystem.cpp/.hpp: 0
- LegacyCharOf bridge points in GayaSystem entity-entry functions: 16

What went smoothly
- Public GayaSystem surface was already entity-first, so no caller cascade was needed.
- Existing CHARACTER wrappers made the migration low-risk.
- LegacyCharPtr component was already available and stable after VID-A.

Unexpected friction
- char_event_info::ch is DynamicCharacterPtr, not a raw CHARACTER pointer.
  - Correct access is info->ch.Get().
- MSVC rejected mixed auto return deduction for pointer + nullptr in the local helper.
  - Fixed by using an explicit LegacyCharHandle alias.

Recommendation for next targets
- AffectSystem
  - Good next candidate if handled as targeted helper cleanup first.
  - It already mixes ECS events with legacy pointers and has similar bridge opportunities.
- SkillSystem
  - Higher risk than GayaSystem.
  - Better after one or two more small/medium pilots, because it has deeper combat/runtime coupling.

Recommendation for Phase 15B reuse
- Prefer entity-first public signatures.
- Keep legacy authority behind exactly one local bridge per function.
- Replace entity -> Find(...) with entity -> LegacyCharPtr wherever state authority still lives on CHARACTER.
- Do not attempt ECS component expansion in the same pass as bridge cleanup.


Phase 15B-2 — AffectSystem entity-first cleanup

Scope
- File: SRC/Server/GameServer/ecs/systems/AffectSystem.cpp
- No public API expansion was needed in AffectSystem.hpp because the AffectSystem surface was already entity-first.

Audit summary
- Baseline raw LPCHARACTER / CHARACTER* token count in AffectSystem.cpp/.hpp: 32
- External AffectSystem::* callers outside the file: 2
  - main.cpp
    - UpdateAffect(g_registry, tick)
    - AffectSystem_Update(g_registry, tick)
- Direct gameplay callers still primarily use CHARACTER wrapper methods inside the same file:
  - AttackedByFire
  - AttackedByPoison
  - AttackedByBleeding
  - RemoveFire / RemovePoison / RemoveBleeding
  - IsImmune
  - ApplyMobAttribute

Signature decisions
- All AffectSystem namespace functions stayed OPTION_ENTITY.
- No KEEP_LEGACY_OVERLOAD was required.
- The only LPCHARACTER survivors after the pass are CHARACTER wrapper signatures:
  - void CHARACTER::AttackedByFire(LPCHARACTER ...)
  - void CHARACTER::AttackedByPoison(LPCHARACTER ...)
  - void CHARACTER::AttackedByBleeding(LPCHARACTER ...)

Implementation pattern summary
- Pattern A (read from ECS component instead of CHARACTER): 0
  - Affect state still ultimately mirrors the legacy affect container.
  - No new affect ECS component authority was introduced in this pass.

- Pattern B (legacy mutating authority through one resolved CHARACTER bridge): 10
  - ApplyFire
  - RemoveFire
  - ApplyPoison
  - RemovePoison
  - ApplyBleeding
  - RemoveBleeding
  - AddAffect
  - RemoveAffect
  - ClearAffect
  - RefreshAffect

- Pattern C (legacy packet/chat/event interaction through one resolved CHARACTER bridge): 8
  - poison_event
  - bleeding_event
  - fire_event
  - IsImmune
  - ApplyMobAttribute
  - affect_event
  - load_affect_login_event
  - test_server info-chat branches in poison/bleed/fire application

- Pattern D (resolve legacy pointer once at top of entity-entry function): 13
  - All AffectSystem namespace entity-entry functions now use a single:
    auto* ch = LegacyCharOf(entity);

Concrete changes
- Replaced local entity -> CHARACTER resolution from:
  - VIDComponent + CHARACTER_MANAGER::Find(...)
  to:
  - LegacyCharPtr component lookup
- Converted raw LPCHARACTER locals to `auto*` bridge locals.
- Converted DynamicCharacterPtr event access from:
  - info->ch
  to:
  - info->ch.Get()
- Converted registry iteration from:
  - view<AffectList, VIDComponent> + Find(vid.value)
  to:
  - view<AffectList, LegacyCharPtr> + legacy.ptr

Lifecycle safety findings
- Affect event callbacks already had the correct null-safe shape:
  - if event info is null -> return 0
  - if resolved character is null -> return 0
- The critical teardown-safe pattern here is:
  - resolve from DynamicCharacterPtr or LegacyCharPtr
  - null-check before dereference
  - avoid re-looking up through CHARACTER_MANAGER when the entity already carries LegacyCharPtr
- No extra ECS-side cleanup path was introduced because AffectList still mirrors legacy affect ownership.

Counts
- Baseline LPCHARACTER count from audit: 32
- Post-migration raw LPCHARACTER / CHARACTER* token count in AffectSystem.cpp/.hpp: 3
- Intentional survivors: 3
  - all three are CHARACTER wrapper signatures for attacker input
- Accidental survivors: 0

What went smoothly
- AffectSystem namespace API was already entity-first, so there was no caller cascade.
- LegacyCharPtr again proved to be the correct bridge after VID-A.
- The event/lifecycle code already had most of the required null checks; the main cleanup was the lookup path, not the control flow.

Unexpected friction
- The baseline count was higher than the old audit estimate because this file still contains large legacy CHARACTER-affect implementations below the AffectSystem namespace.
- The important distinction was:
  - AffectSystem namespace internals
  versus
  - legacy CHARACTER wrapper / implementation code living in the same compilation unit

Verification
- Build gate: PASS
- WinTest deploy + restart: PASS
- Boot/log smoke: PASS
- No new AffectSystem-specific syserr was observed during the fresh boot window.
- Full in-client affect gameplay validation still needs manual smoke for:
  - buff icon appearance
  - duration countdown
  - death/logout restore paths

Recommendation for next targets
- SkillSystem
  - next logical step, but higher risk than AffectSystem
  - should reuse the same LegacyCharPtr bridge cleanup first
- CombatSystem
  - defer until after one more medium-risk successful system pass


Phase 15B-3 - SkillSystem entity-first cleanup

Scope
- File: SRC/Server/GameServer/ecs/systems/SkillSystem.cpp
- SkillSystem.hpp needed no signature expansion in this pass.
- Existing VID-A6 groundwork remained in place:
  - CheckSkillHit(attacker, skillId, target) already entity-based
  - CheckSkillHitCount already entity-based
  - TSkillUseInfo::dwVID is entt::entity
  - TSkillUseInfo::target_map is std::map<entt::entity, size_t>

Audit summary
- Baseline raw LPCHARACTER / CHARACTER* token count in SkillSystem.cpp/.hpp: 42
- External direct SkillSystem::* callers outside the file were effectively negligible.
- The practical entry chain still comes through CHARACTER wrapper methods such as:
  - UseSkill
  - ComputeSkill
  - ComputeSkillParty
  - ComputeGyeongGongSkill
- High-risk internal hotspots before cleanup were:
  - LegacyCharacter(entity) using VIDComponent + CHARACTER_MANAGER::Find(...)
  - chain_lightning_event payload using runtime ids resolved later
  - splash functors carrying LPCHARACTER state
  - event callbacks reading DynamicCharacterPtr as raw pointer

Signature decisions
- Namespace-level entity-entry helpers stayed OPTION_ENTITY.
- No wide external caller cascade was needed.
- Large CHARACTER runtime wrappers stayed intentionally legacy for now:
  - int CHARACTER::ComputeSkillParty(uint32_t, LPCHARACTER, uint8_t)
  - int CHARACTER::ComputeGyeongGongSkill(uint32_t, LPCHARACTER, uint8_t)
  - int CHARACTER::ComputeSkill(uint32_t, LPCHARACTER, uint8_t)
  - bool CHARACTER::UseSkill(uint32_t, LPCHARACTER, bool)
- These 4 wrappers are the only raw LPCHARACTER survivors after the pass.

Implementation pattern summary
- Pattern A (pure ECS reads replacing CHARACTER reads): 0
  - Skill authority still ultimately flows through legacy CHARACTER runtime logic.
  - This pass only removed the old VID-based bounce-back lookup path.

- Pattern B (legacy mutating authority through one resolved CHARACTER bridge): 9
  - IsLearnableSkill
  - LearnGrandMasterSkill
  - LearnSkillByBook
  - CanUseMobSkill
  - CanUseSkill
  - CheckSkillHit
  - ComputeCooltime
  - ResetSkill
  - chain_lightning_event victim/caster resolution

- Pattern C (legacy packet/event/combat interaction through one resolved CHARACTER bridge): 6
  - skill_muyoung_event
  - skill_gyeongGong_event
  - mob_skill_hit_event
  - chain_lightning_event
  - FuncSplashDamage
  - FuncSplashAffect

- Pattern D (resolve legacy pointer once at top of entity-entry function): 11
  - All cleaned entity-entry helpers now use a single LegacyCharOf(entity) lookup.

Concrete changes
- Replaced local entity -> CHARACTER resolution from:
  - VIDComponent + CHARACTER_MANAGER::Find(...)
  to:
  - LegacyCharPtr component lookup
- Added a local bridge alias:
  - using LegacyCharHandle = decltype(std::declval<ecs::LegacyCharPtr>().ptr);
- Updated helper/functor members and parameters from LPCHARACTER to LegacyCharHandle:
  - FFindNearVictim
  - FuncSplashDamage
  - FuncSplashAffect
  - FComputeSkillParty
  - FHealerParty
- Converted chain_lightning_event payload from runtime uint32 ids to entt::entity:
  - dwVictim: entt::entity
  - dwChr: entt::entity
- Converted event callback access from:
  - info->ch
  to:
  - info->ch.Get()
- Removed unnecessary const_cast in CHARACTER const wrappers by using:
  - AIHelpers::EcsOf(this)

Lifecycle / null-safety findings
- Skill teardown edges are more dangerous than Gaya or Affect because both attacker and victim may disappear mid-chain.
- The safe pattern for this pass is:
  - resolve attacker once
  - resolve target once
  - abort or skip when either side is missing
- chain_lightning_event is now safer because it no longer stores packet ids that must be re-routed through a later Find(...).
- DynamicCharacterPtr event payloads must always be read with .Get() before dereference.

Counts
- Baseline LPCHARACTER count from audit: 42
- Post-migration raw LPCHARACTER / CHARACTER* token count in SkillSystem.cpp/.hpp: 4
- Intentional survivors: 4
  - all four are large CHARACTER wrapper signatures listed above
- Accidental survivors: 0
- LegacyCharOf bridge points in SkillSystem.cpp/.hpp: 11

What went smoothly
- VID-A6 groundwork had already converted target tracking and hit-count bookkeeping to entt::entity.
- LegacyCharPtr again proved to be the right bridge after VID elimination.
- Helper/functor cleanup was straightforward once the old Find(...) bounce was removed.

Unexpected friction
- The baseline token count was materially higher than the original phase estimate because SkillSystem.cpp still hosts a very large amount of legacy CHARACTER runtime code.
- The file mixes three layers in one compilation unit:
  - entity-entry helpers
  - event/functor glue
  - legacy CHARACTER skill runtime
- That means token-count reduction is useful, but must be interpreted carefully.

Verification
- Build gate: PASS
- WinTest deploy + restart: PASS
- Boot/log smoke: PASS
- No fresh skill-specific null-deref, VID_DRIFT, DestroyCharacter, or SpawnMob regression was observed in the fresh boot window.
- Full in-client skill gameplay still requires manual smoke for:
  - single-target skill damage
  - AoE skill damage
  - buff/debuff application
  - death/logout/channel edge cases

Cumulative 15B progress so far
- GayaSystem: 17 -> 0
- AffectSystem: 32 -> 3
- SkillSystem: 42 -> 4

Recommendation for next targets
- CombatSystem
  - next logical high-value target, but higher risk than SkillSystem because it owns the core runtime combat lifecycle
- MountSystem / ItemSystem
  - lower-risk alternatives if a smaller stabilization pass is preferred before CombatSystem


Phase 15B-4 - MountSystem entity-first cleanup

Scope
- File: SRC/Server/GameServer/ecs/systems/MountSystem.cpp
- MountSystem.hpp was already entity-first and needed no signature expansion.
- This pass focused on the ECS-facing bridge layer, not on the large legacy CHARACTER mount runtime surface in the same file.

Audit summary
- Baseline raw LPCHARACTER / CHARACTER* token count in MountSystem.cpp/.hpp: 22
- Public MountSystem namespace API was already entity-first:
  - StartRiding
  - StopRiding
  - SetRider
  - GetRider
  - HorseSummon
  - GetMyHorseVnum
  - HorseDie
  - ReviveHorse
  - ClearHorseInfo
  - SendHorseInfo
  - CanUseHorseSkill
  - SetHorseLevel
  - IsRiding
  - GetMountVnum
- External dependency audit showed mount state is read widely through legacy CHARACTER methods:
  - battle.cpp
  - input_main.cpp
  - ani.cpp
  - cmd_emotion.cpp
  - guild_war.cpp
  - questlua_pc.cpp
- Mount lifecycle actions are still triggered through legacy callers such as:
  - cmd_general.cpp
  - ItemSystem.cpp
- There was no ECS registry view loop to convert in this file; the main problem was the old bridge helper and event dereference pattern.

Signature decisions
- All MountSystem namespace functions stayed OPTION_ENTITY.
- No KEEP_LEGACY_OVERLOAD was required.
- The remaining LPCHARACTER signatures after the pass are intentional CHARACTER wrapper/runtime surfaces:
  - void CHARACTER::UpdateMountInventoryCountOverhead(LPCHARACTER viewer)
  - void CHARACTER::SetRider(LPCHARACTER ch)
  - LPCHARACTER CHARACTER::GetRider() const

Implementation pattern summary
- Pattern A (pure ECS reads replacing CHARACTER reads): 0
  - Mount authority still ultimately flows through legacy CHARACTER / CHorseRider / CMountSystem runtime.

- Pattern B (legacy mutating authority through one resolved CHARACTER bridge): 12
  - StartRiding
  - StopRiding
  - SetRider
  - GetRider
  - HorseSummon
  - GetMyHorseVnum
  - HorseDie
  - ReviveHorse
  - ClearHorseInfo
  - SendHorseInfo
  - CanUseHorseSkill
  - SetHorseLevel

- Pattern C (legacy packet/event/lifecycle interaction through one resolved CHARACTER bridge): 3
  - IsRiding
  - GetMountVnum
  - horse_dead_event

- Pattern D (resolve legacy pointer once at top of entity-entry function): 14
  - All cleaned MountSystem namespace entry points now use a single LegacyCharOf(entity) lookup.

Concrete changes
- Replaced local entity -> CHARACTER resolution from:
  - VIDComponent + CHARACTER_MANAGER::Find(...)
  to:
  - LegacyCharPtr component lookup
- Added a local bridge alias:
  - using LegacyCharHandle = decltype(std::declval<ecs::LegacyCharPtr>().ptr)
- Converted namespace-level LPCHARACTER locals to `auto*` bridge locals.
- Converted horse_dead_event from:
  - info->ch
  to:
  - info->ch.Get()
- Removed stray local CHARACTER* cast in viewer-update code by reusing LegacyCharHandle.

Lifecycle / null-safety findings
- MountSystem has a lighter ECS-facing lifecycle surface than SkillSystem because the heavy mount runtime still lives in CHARACTER methods.
- The risky event path here was horse_dead_event; it now reads DynamicCharacterPtr safely through `.Get()`.
- Rider/mount dual-entity handling is still mostly encapsulated in legacy runtime classes, so this pass intentionally cleaned only the ECS-facing bridge layer.
- This means warp/death/logout mount behavior still depends on legacy runtime correctness, but the old entity -> VID -> Find bounce-back path is gone from the ECS entry points.

Counts
- Baseline LPCHARACTER count from audit: 22
- Post-migration raw LPCHARACTER / CHARACTER* token count in MountSystem.cpp/.hpp: 3
- Intentional survivors: 3
  - all three are wrapper/runtime signatures listed above
- Accidental survivors: 0
- LegacyCharOf bridge points in MountSystem.cpp/.hpp: 16

What went smoothly
- MountSystem namespace API was already entity-first, so there was no caller cascade.
- LegacyCharPtr again removed the old VIDComponent + Find bridge cleanly.
- The file had no ECS view iteration to rewrite, which kept the blast radius small.

Unexpected friction
- The baseline token count was higher than the old audit estimate because MountSystem.cpp also contains a large amount of classic CHARACTER mount/inventory runtime code.
- The file mixes:
  - ECS-facing MountSystem namespace entry points
  - event glue
  - legacy CHARACTER mount runtime
- As with AffectSystem and SkillSystem, token-count reduction is meaningful only when separating those layers.

Verification
- Build gate: PASS
- WinTest deploy + restart: PASS
- Boot/log smoke: PASS
- No fresh mount-lifecycle syserr was observed after restart.
- Only the usual pre-existing mount motion/content asset warnings remained.
- Full in-client mount gameplay still needs manual smoke for:
  - ride / dismount
  - mounted melee and mounted skill use
  - death while mounted
  - warp while mounted
  - logout/login while mounted

Cumulative 15B progress so far
- GayaSystem: 17 -> 0
- AffectSystem: 32 -> 3
- SkillSystem: 42 -> 4
- MountSystem: 22 -> 3
- Total reduction so far: 107 -> 10

Recommendation for next targets
- CombatSystem
  - highest value next target, but materially higher risk because it owns the core melee lifecycle
- ItemSystem
  - lower-risk alternative if another stabilization pass is preferred before CombatSystem


Phase 15B-5 - CombatSystem entity-first cleanup

Scope
- File: SRC/Server/GameServer/ecs/systems/CombatSystem.cpp
- CombatSystem.hpp needed no signature expansion in this pass.
- This pass intentionally did not collapse the large legacy CHARACTER combat wrapper surface.
- The target was the accidental LPCHARACTER-heavy helper/event/iteration layer around that wrapper surface.

Audit summary
- Baseline raw LPCHARACTER / CHARACTER* token count in CombatSystem.cpp/.hpp: 154
- This is much higher than the old phase-15 baseline estimate because the file now contains:
  - ECS entry helpers
  - aggro / reward / drop helper structs
  - event handlers
  - large legacy CHARACTER combat runtime wrappers
- Already-entity groundwork from previous phases remained in place before this pass:
  - TDamageMap keyed by entt::entity
  - m_eVictim is entt::entity
  - dead_event payload uses entt::entity
- One stale ECS view still used VIDComponent before this pass:
  - CombatSystem_Update view<..., VIDComponent, ...>

Signature decisions
- CombatSystem namespace entry helpers stayed OPTION_ENTITY.
- CHARACTER:: combat wrappers intentionally stayed KEEP_LEGACY_OVERLOAD:
  - Attack
  - Damage
  - Dead
  - BeginFight
  - DistributeHP
  - DistributeSP
  - ItemDropPenalty
  - RewardGold
  - GetVictim / SetVictim / GetNearestVictim
  - aggro update wrappers
- Internal static helper functions, helper structs, lambdas, and event handlers were converted away from LPCHARACTER where possible.

Implementation pattern summary
- Pattern A (pure ECS reads replacing CHARACTER reads): 1
  - CombatSystem_Update view switched from VIDComponent to LegacyCharPtr.

- Pattern B (legacy mutating authority through one resolved CHARACTER bridge): many helper-entry sites
  - top CombatSystem namespace wrappers now resolve through LegacyCharPtr instead of VIDComponent + Find
  - drop / exp / reward helper functions use LegacyCharHandle internally

- Pattern C (event/callback payload and iteration safety): several
  - dead_event already entity-based and stayed so
  - helper functors now carry LegacyCharHandle instead of LPCHARACTER
  - for_each_pc / dungeon-member lambdas no longer use raw LPCHARACTER type declarations internally

- Pattern D (resolve once, then stay on bridge handle): dominant pattern in this pass
  - LegacyCharHandle alias used for helper structs, temporary locals, containers, and utility function params

Concrete changes
- Replaced local entity -> CHARACTER resolution from:
  - VIDComponent + CHARACTER_MANAGER::Find(...)
  to:
  - LegacyCharPtr component lookup
- Added a local bridge alias:
  - using LegacyCharHandle = decltype(std::declval<ecs::LegacyCharPtr>().ptr)
- Converted top CombatSystem namespace helper layer from raw LPCHARACTER locals to `auto*` bridge locals.
- Converted helper structs from LPCHARACTER storage to LegacyCharHandle:
  - FuncForgetMyAttacker
  - FuncAggregateMonster
  - FuncAggregateMonsterPlus
  - FuncAttractRanger
  - FuncPullMonster
  - NPartyExpDistribute::FPartyTotaler
  - NPartyExpDistribute::FPartyDistributor
  - SDamageInfo
  - CFuncShoot
  - FuncSetLastAttacked
  - FuncDeadSpawnedByStone
- Converted internal helper signatures from LPCHARACTER to LegacyCharHandle where safe:
  - AdjustExpByLevel_Combat
  - GiveExp
  - __UpdateBattlePassCollectProgress
  - __TryAutoGiveRewardItem
  - __GiveRewardItemToCharacterOrDrop
  - MakeItemLink
  - ProcessStoneSpawnStep
  - CalcReference* damage helpers
- Converted the remaining stale ECS runtime view from:
  - view<..., VIDComponent, ...>
  to:
  - view<..., LegacyCharPtr, ...>

Death / drop / quest chain notes
- The death-chain authority remained on the existing legacy wrapper flow, but helper/iteration resolution is now entity-first.
- End-to-end chain ownership after this cleanup:
  - death scheduling: dead_event with entt::entity payload (already in place before this pass)
  - damage ownership: TDamageMap keyed by entt::entity (already in place before this pass)
  - exp distribution: entity-key iteration resolves attackers through LegacyCharPtr bridge
  - drop attribution: reward helper queues/vectors now carry LegacyCharHandle, not LPCHARACTER declarations
  - quest / corpse / post-death flow: unchanged authority, but no reintroduced VID bounce-back path was added
- No duplicate VIDComponent + Find lookup path remains in CombatSystem internals after this pass.

Counts
- Baseline LPCHARACTER count from audit: 154
- Post-migration raw LPCHARACTER / CHARACTER* token count in CombatSystem.cpp/.hpp: 17
- Intentional survivors: 17
  - all remaining matches are large CHARACTER wrapper/runtime signatures
- Accidental internal survivors: 0

What went smoothly
- VID-A groundwork had already eliminated the hardest identity problems.
- LegacyCharPtr again removed the old bounce-back lookup path cleanly.
- Helper-struct and utility-function cleanup yielded a large reduction without touching the most dangerous combat wrapper signatures.

Unexpected friction
- CombatSystem baseline was far larger than expected because the file is effectively several subsystems in one compilation unit.
- The safe migration boundary here is not file-level; it is layer-level:
  - helper/event/iteration layer can be cleaned now
  - core CHARACTER combat wrappers must stay legacy-signature for now
- This is the opposite signature ratio from GayaSystem, where nearly everything could go entity-first directly.

Verification
- Build gate: PASS
- WinTest deploy + restart: PASS
- Boot/log smoke: PASS
- No fresh combat-system startup regression was observed:
  - no VID_DRIFT
  - no DestroyCharacter not found
  - no SpawnMob cannot create new character
- Only the usual pre-existing motion/content warnings remained.
- Full in-client combat regression is still required for:
  - normal melee kill
  - skill kill
  - AoE splash kill
  - exp/drop/quest chain
  - death and corpse cleanup
  - PvP / party assist / guild-war related combat

Cumulative 15B progress so far
- GayaSystem: 17 -> 0
- AffectSystem: 32 -> 3
- SkillSystem: 42 -> 4
- MountSystem: 22 -> 3
- CombatSystem: 154 -> 17
- Total reduction so far: 267 -> 27

Recommendation after this pass
- ItemSystem is now the most reasonable next 15B target if another medium-risk cleanup is preferred.
- If gameplay remains stable after manual combat validation, the project is in a good position to pivot from 15B system-internal cleanup toward the next broader LPCHARACTER reduction phase.
