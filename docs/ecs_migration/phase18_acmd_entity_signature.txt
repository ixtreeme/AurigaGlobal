Phase 18 - ACMD command handler entity signature flip
Date: 2026-09-01

Scope
-----
Continuation of the "OOP - ECS konverzio eltuntetes" batches (d8ec8cb, 6cc076f).
Those commits inlined AIHelpers::EcsOf(x) into the explicit form
((x) ? (x)->GetEntityHandle() : entt::null) so every remaining OOP->ECS
conversion point became greppable. This phase removes that conversion from the
whole chat-command surface by pushing the entity up into the handler signature.

Signature change
----------------
cmd.h:
  #define ACMD(name)  void (name)(LPCHARACTER ch, ...)
    ->
  #define ACMD(name)  void (name)(entt::entity character, ...)

  struct command_info::command_pointer follows the same flip.

cmd.cpp:
  interpret_command(entt::entity, ...) is now the primary implementation; it
  resolves the legacy pointer once (ecs::LegacyCharOf) for the anti-flood /
  position gates and dispatches the entity to the handler.
  interpret_command(LPCHARACTER, ...) is now a one-line wrapper kept for the
  remaining legacy call sites.

Handler body rule
-----------------
Inside each ACMD body:
  - ((ch) ? (ch)->GetEntityHandle() : entt::null)  ->  character
  - handlers that still call legacy CHARACTER methods get exactly one alias at
    the top of the body:
        LPCHARACTER ch = ecs::LegacyCharOf(character);
    so every remaining ch-> call site is textually unchanged and the legacy
    surface stays trivially greppable for the next phase.

Files and counts
----------------
  cmd.cpp                 7 handlers,   15 conversions removed,   5 legacy refs
  cmd_gm.cpp            121 handlers,  354 conversions removed, 234 legacy refs
  cmd_gm2.cpp             2 handlers,    6 conversions removed,   0 legacy refs
  cmd_general.cpp        78 handlers,  384 conversions removed, 282 legacy refs
  cmd_general2.cpp       14 handlers,  154 conversions removed,  34 legacy refs
  cmd_emotion.cpp         2 handlers,   17 conversions removed,  11 legacy refs
  cmd_oxevent.cpp         3 handlers,    5 conversions removed,   1 legacy ref
  cmd_guild_renewal.cpp   6 handlers,    9 conversions removed,  11 legacy refs

  Total: 233 handlers, 944 conversion sites removed, 578 legacy refs aliased
         into 147 single-line ch aliases.

Project-wide conversion count (grep "GetEntityHandle() : entt::null"):
  before: 6354
  after:  5400

Special cases handled by hand
-----------------------------
- cmd_gm.cpp do_full_set: the three local `extern void do_xxx(LPCHARACTER ...)`
  redeclarations were replaced by `extern ACMD(do_xxx);` so they track the macro.
- cmd_gm.cpp block_chat(entt::entity): now forwards the entity directly instead
  of round-tripping through ecs::LegacyCharOf.
- cmd.cpp / cmd_gm.cpp: three handlers already declared a local
  `const entt::entity character = ...`; those declarations are now redundant and
  were deleted (they collided with the new parameter name).
- cmd_gm.cpp do_save_attribute_to_image carries a trailing comment on the ACMD
  line; it needed the same treatment as the rest.
- cmd_oxevent.cpp / cmd_guild_renewal.cpp gained
  #include "ecs/CharacterAccessors.hpp" for ecs::LegacyCharOf.

Deliberately left alone
-----------------------
41 conversion sites remain in the cmd_*.cpp files. They are NOT in ACMD bodies:
  - file-local helpers that still take LPCHARACTER
    (Command_ApplyAffect, CHARACTER_GoToName, MakeDailyRewardHWKey,
     DailyReward_CheckHWIDLimit, the FCountInMap / FuncPurge / FuncWeaken
     functors, open_in_game_mall's neighbours)
  - commented-out blocks in cmd_gm.cpp
These are the natural next slice for this file group.

Next targets by conversion density
----------------------------------
  input_main.cpp                   465   (CInputMain packet handlers, uniform
                                          LPCHARACTER ch first parameter,
                                          single dispatcher in Analyze())
  ecs/systems/CombatSystem.cpp     419
  ecs/systems/ItemSystem_LegacyBridge.cpp 298
  char_manager.cpp                 204
  battle.cpp                       172

Build status
------------
cmake --build build --config RelWithDebInfo --target GameServer --parallel 8
Result: build succeeded, 0 errors, GameServer.exe linked.


Phase 18b - CInputMain packet handler entity signature flip
-----------------------------------------------------------
Same rule applied to the client packet handler surface.

input.h:
  64 CInputMain member declarations flipped from LPCHARACTER <p> to
  entt::entity character (first parameter only). <entt/entt.hpp> added.

input_main.cpp:
  63 handler definitions transformed; 460 conversion sites removed;
  265 legacy refs kept alive through 53 single-line
  "LPCHARACTER <p> = ecs::LegacyCharOf(character);" aliases.

  CInputMain::Analyze and CInputDead::Analyze keep their LPCHARACTER ch (they
  still need it for the legacy shop/safebox/dragon-soul gates and for
  CWhisperAdmin::Manager / CInputProcessor::Version / OfflineshopPacket, which
  are not migrated yet). They now resolve the entity once:
      const entt::entity character = ch->GetEntityHandle();
  and dispatch that to the 67 handler call sites; 22 further conversions inside
  the dispatcher collapsed onto the same local.

  63 conversion sites remain in input_main.cpp; none are in a CInputMain
  handler - they are file-local helpers and other subjects (target, pkChr,
  d->GetCharacter()).

Project-wide conversion count: 5400 -> 4917
Build: RelWithDebInfo GameServer, 0 errors, first attempt.


Phase 18c - CHARACTER_MANAGER consumer API entity flip
-------------------------------------------------------
char_manager.cpp had 219 conversion sites (the where.py breakdown counts both
the ((p) ? ...) and the (p ? ...) spellings). They split into two kinds, and
each kind got the treatment it deserves.

A. Signature flip - methods that only *consume* a character
   14 declarations in char_manager.h moved to entt::entity:

     CheckEventForDrop(pkChr, pkKiller, ...)  -> (character, killer, ...)   96
     LoadItemShopBuyReal                                                     8
     LoadItemShopBuy                                                         6
     LoadItemShopLog                                                         3
     LoadItemShopLogReal                                                     2
     LoadItemShopData(ch, isAll)                                             2
     PacketMonsterLog                                                        3
     SendDataPlayer                                                          2
     CheckBonusEvent                                                         2
     GetMobDamageRate / GetMobGoldAmountRate / GetMobGoldDropRate /
     GetMobExpRate / GetUserDamageRate                                       6

   CheckEventForDrop alone was 96 of them - 93 repetitions of the same
   ((pkChr) ? ...) inside one 380-line boss/metin drop-event chain.

   The four premium-gated rate getters were rewritten rather than aliased:
   `ch->GetPremiumRemainSeconds(X)` is already a pure forward to
   ecs::PlayerRuntime::GetPremiumRemainSeconds, which returns 0 for a null or
   invalid entity - so the `if (!ch) return <base rate>;` guard was exactly
   redundant with the premium check and could go.

   GetMobItemRate(LPCHARACTER) was a dead forwarder to the entity overload that
   already existed; its only caller already passed an entity. Deleted.

   62 external call sites were checked; only 14 actually needed touching
   (battle.cpp, cmd_general.cpp, CombatSystem.cpp x9, PlayerRuntimeSystem.cpp,
   StatSystem.cpp, input_db.cpp x2, input_login.cpp, item_manager.cpp). Where
   the caller still holds a real pointer the conversion moved outward to it -
   that is the point: it now sits at the one place a pointer still lives.

B. Hoist - functions that own a local pointer and re-converted it N times
   No signature change; the repeated conversion collapses onto one local:

     SpawnMob 12, SpawnMobRandomPosition 11, DestroyCharacter 8, Update 6,
     SpawnMoveGroup 5, SpawnGroup 4, SpawnMobRange 3, FindSpecifyPC 3,
     the anonymous-namespace alive-VID sweep 3, FindByPID 2, FindPC 2,
     SetEventStatus 2, RegisterRaceNumMap 2, UnregisterRaceNumMap 2,
     RefreshItemShop 1

   These are factory / lookup / lifetime functions. Their LPCHARACTER is the
   legacy identity boundary itself, so flipping their signature belongs to a
   later identity phase, not to this one.

Left in place (25 sites)
------------------------
  - 18 are LPITEM subjects (rewardItem, extraDrop, item) - a different axis.
  - 3 are the `c` loop variable over m_set_pkChrMonsterLog, a pointer set.
  - 4 are lone conversions inside ENABLE_INGAME_DEBUG_RAZOR93 blocks in
    AddToStateList / RemoveFromStateList / RegisterForMonsterLog /
    UnregisterForMonsterLog. Those methods genuinely operate on CHARACTER_SET
    pointer containers; flipping them would only add a LegacyCharOf round-trip.

char_manager.cpp: 219 -> 25 conversion sites
Project-wide conversion count: 4917 -> 4743
Build: RelWithDebInfo GameServer, 0 errors, first attempt.


Phase 18d - battle.h / battle.cpp full entity flip
---------------------------------------------------
The whole battle API is now entity-native. This is the first file in the
migration that ends at *zero* inlined conversions, header included.

battle.h - 14 declarations + 2 inline definitions flipped:
  CalcAttBonus, CalcMeleeDamage, CalcMagicDamage, CalcArrowDamage,
  CalcAttackRating, battle_is_attackable, battle_melee_attack, battle_end,
  battle_distance_valid, NormalAttackAffect, and the ENABLE_ANTICHEAT quartet
  GET_ATTACK_SPEED / SET_ATTACK_TIME / SET_ATTACKED_TIME / IS_SPEED_HACK.

  The two inline helpers AttackAffect (11 conversions) and SkillAttackAffect
  (5) had *zero* legacy pointer uses in their bodies - pure flips, no alias
  needed. They are now entity-only from signature to last statement.

battle.cpp - 19 definitions, 195 conversion sites removed:
  CalcAttBonus 56, battle_hit 36, battle_melee_attack 24, CalcMeleeDamage 23,
  CalcArrowDamage 18, battle_is_attackable 17, IS_SPEED_HACK 10,
  NormalAttackAffect 8, battle_distance_valid 4, plus 9 smaller ones.

Naming convention settled here
------------------------------
After this pass, inside the battle code:
  attacker / victim / character  always mean entt::entity
  pkAttacker / pkVictim / ch     always mean LPCHARACTER
Functions whose parameter was already named `victim` as a pointer had their
remaining pointer uses renamed to pkVictim, so the entity could take the good
name. That rename is compiler-checked in both directions: entt::entity is an
enum class, so it has no operator-> and no implicit conversion to a pointer -
any mix-up is a hard error, never a silent behaviour change.

Call sites
----------
41 external call sites in 5 files were adapted (cmd_gm.cpp, AffectSystem.cpp,
CombatSystem.cpp, NetworkSyncSystem.cpp, SkillSystem.cpp) plus 24 intra-file
calls. Each file kept its own local spelling of the conversion.
NetworkSyncSystem.cpp carried a private `extern bool battle_is_attackable(
LPCHARACTER, LPCHARACTER);` redeclaration that had to move with the header.

Tooling note
------------
The parameter-flip helper had a placeholder collision: it substituted each
conversion with "\0<entityName>\0", so when a parameter was being renamed to
its own name (victim -> victim) the later bare-use scan matched the
placeholders themselves and rewrote entity slots back into pointer uses. The
first battle.cpp run was reverted and the helper switched to opaque, indexed
placeholders. Earlier phases were unaffected: their entity names (character,
killer, attacker) never matched their parameter names (ch, pkChr, pkKiller,
pkAttacker) as whole words.

Left in place
-------------
  - battle.h still declares `extern int battle_count_attackers(LPCHARACTER ch);`
    That function has no definition and no caller anywhere in the tree; it is a
    dead declaration, not a migration target.
  - one conversion inside a commented-out line in CalcArrowDamage, on an LPITEM
    subject (pkBow).

battle.cpp: 195 -> 0 (1 in a comment)   battle.h: 16 -> 0
Project-wide conversion count: 4743 -> 4568
Build: RelWithDebInfo GameServer, 0 errors.


Phase 18e - guild and pvp subsystem entity flip
------------------------------------------------
Two subsystems in one pass, same rule as before.

guild
-----
guild.h: 24 declarations flipped (one deleted, see below).
  LoginMember, LogoutMember, SendAllGradePacket, SendListPacket,
  SendListOneToAll, SendLoginPacket x2, SendLogoutPacket x2,
  SendGuildInfoPacket, DeleteComment, RefreshComment, UseSkill,
  SendSkillInfoPacket, ChargeSP, RequestDepositMoney, RequestWithdrawMoney,
  SendEnemyGuild, Invite, InviteAccept, VerifyGuildJoinableCondition,
  GiveGuildBuff, RemoveGuildBuff.
guild_manager.h: CGuildManager::LoginMember and RemoveGuildBuff too, so the
  whole guild subsystem speaks one language.
guild_war.cpp: CGuild::SendEnemyGuild lives there; it moved with the header.

  CGuild::RequestAddMember(LPCHARACTER) was a forwarder to the entity overload
  that already existed. Its last caller was Invite/InviteAccept's pchInvitee,
  which is now an entity, so the forwarder was deleted. Same shape as
  GetMobItemRate in phase 18c.

  Two (entity, entity) forwarders - SendLoginPacket and SendLogoutPacket -
  no longer need a legacy alias at all; they hand the entity straight to the
  (entity, pid) overload. GiveGuildBuff likewise.

  guild.cpp: 104 -> 24 by the strict per-parameter count.

pvp
---
pvp.h: 9 declarations flipped (one deleted).
  Insert, CanAttack, Dead, GiveUp, Connect, Decline, Disconnect, ConnectEx,
  plus the file-local RemoveStateFull helper.
  CPVPManager::IsFighting(LPCHARACTER) was another dead forwarder to the
  IsFighting(uint32_t) overload - every caller in the tree already passed a
  PID. Deleted.
  <entt/entt.hpp> added to pvp.h.

  CPVPManager::Insert alone was 45 conversions (31 pkChr + 14 pkVictim) with
  *zero* legacy pointer uses in its body - a pure flip.

  The two duel EVENTFUNCs are the other half: pvp_duel_counter (54) and
  pvp_check_disconnect (12) pull chA/chB out of the event info struct and then
  re-converted them on every line. They now resolve once into characterA /
  characterB. Both keep the null-safe form, because pvp_check_disconnect
  deliberately runs with one side already gone.

  pvp.cpp: 163 -> 8, and 6 of those 8 *are* the hoisted locals - the single
  point where a pointer becomes an entity. Only 2 real sites remain
  (pkVictimDesc, and d->GetCharacter() in SendList).

std::bind caught what the eye missed
------------------------------------
Seven for_each/std::bind sites in guild.cpp bound a now-entity member function
over m_memberOnline, which is a CHARACTER_SET:

  for_each(m_memberOnline.begin(), m_memberOnline.end(),
           std::bind(&CGuild::SendSkillInfoPacket, this, _1));

std::bind defers the type check into the binder's operator(), so this reports
as "attempting to reference a deleted function" deep inside <algorithm> with
no line number in our own code. Each one was rewritten as an explicit loop
that converts at the point of call. Worth remembering for the remaining files:
grep for std::bind over any method being flipped, the compiler will not point
at the call site.

Call sites
----------
21 external call sites adapted across input_main.cpp, input_login.cpp,
battle.cpp, cmd_general.cpp, guild_manager.cpp, AffectSystem.cpp,
CombatSystem.cpp, PlayerRuntimeSystem.cpp, SessionSystem.cpp.

Left in place
-------------
guild.cpp keeps 68 raw conversions, 45 of them on *it / *iter. Those are the
m_memberOnline broadcast loops - the container is a CHARACTER_SET, so it is
the legacy holder, exactly like m_set_pkChrMonsterLog in phase 18c. Turning
those into entity sets is an identity-level change, not this pass.

Project-wide conversion count: 4568 -> 4350
Build: RelWithDebInfo GameServer, 0 errors.


Phase 18f - dungeon group hook API entity flip
-----------------------------------------------
Ten C++ dungeon classes share one hook API. They were migrated as a group.

  CEasterDungeon, CHalloween2022Dungeon, CLostCastleDungeon,
  CNightmareDungeonRazor93, COrcsDungeon, CPyramidDungeonRazor93,
  CRuneDungeon, CTritonTempleDungeon, CValentineDungeon, CVikingDungeon

Common shape:
  OnPlayerLogin(ch) / OnPlayerDisconnect(ch) / OnMobKilled(killer, victim)
  OnClickNpc(ch[, npc]) / OnNpcTakeItem(from, npc, item) / OnUseItem*(ch)
plus LostCastle's CheckCloneDamage(attacker, victim) and
SpawnTestClones(source, target, count).

51 method definitions flipped, 48 declarations across the 10 headers,
49 hook call sites adapted in 6 files:
  trigger.cpp (OnClickNpc x10), input_login.cpp (OnPlayerLogin x10),
  SessionSystem.cpp (OnPlayerDisconnect x9), CombatSystem.cpp (OnMobKilled x11),
  ItemSystem_LegacyBridge.cpp (OnNpcTakeItem / OnUseItem x8), ItemUse.cpp (x1).

Naming rule for this group
--------------------------
These files spell the pointer as `CHARACTER*`, not LPCHARACTER, so the tooling
had to learn both spellings. The entity keeps the semantic name and the legacy
alias takes the codebase's pk prefix:

  ch      -> entity `character`, alias stays `ch` (as in every earlier phase)
  killer  -> entity `killer`,  alias `pkKiller`
  victim  -> entity `victim`,  alias `pkVictim`
  npc / from / attacker / source / target likewise.

Every dungeon .cpp gained #include "ecs/CharacterAccessors.hpp", and every
dungeon .h gained #include <entt/entt.hpp>.

Note on LostCastle
------------------
Every CLostCastleDungeon hook call site in the tree is currently commented out
(trigger.cpp, CombatSystem.cpp, ItemSystem_LegacyBridge.cpp, cmd_gm.cpp). The
class was migrated anyway so the group stays uniform, and the commented call
sites were rewritten with it so they still match if re-enabled.

Dungeon group: 819 -> 351 conversion sites
Project-wide conversion count: 4350 -> 3909
Build: RelWithDebInfo GameServer, 0 errors.

Still open in this group (351 sites), one shape
-----------------------------------------------
Each dungeon file carries its own copy of

  inline void ForEachPcOnMap(int32_t mapIndex,
                             const std::function<void(LPCHARACTER)>& fn)

and the lambdas passed to it re-convert their LPCHARACTER parameter on every
line. Flipping that std::function to void(entt::entity) would push the entity
into every lambda in all ten files at once - that is the natural next step for
this group, and it is a different shape from the hook flip, so it gets its own
pass.


Phase 18g - dungeon group, pass 2: ForEachPcOnMap
--------------------------------------------------
The shape phase 18f left open. Every dungeon file carries its own copy of a
per-map PC iterator, in two spellings:

  A) inline void ForEachPcOnMap(int32_t, const std::function<void(LPCHARACTER)>&)
     (7 files, plus PyramidDungeonRazor93's hand-rolled collector variant)
  B) template <typename F> void ForEachPcOnMap(int32_t, F&&)
     (Halloween2022, RuneDungeon, VikingDungeon)

Both now hand over an entt::entity, converting exactly once at the iterator's
own PC check. 65 lambdas flipped across the ten files, 178 conversion sites
removed inside them; 62 kept a pk-prefixed alias for their remaining pointer
use.

Two follow-on shapes had to be handled
--------------------------------------
1. Named lambdas (applyMember, checkMember, setupMember, prepareMember, fn,
   check, removeEntranceItems) are both passed to ForEachPcOnMap *and* invoked
   directly on the single-player path: `if (!party) applyMember(ch);`. Those
   invocations now pass the entity already in scope.

2. CParty::ForEach*Member still calls back with an LPCHARACTER, and it takes
   Func& - an lvalue. Nine sites in four files handed a now-entity lambda to
   it. Each got a named pointer->entity adapter:

     auto checkMemberPtr = [&](LPCHARACTER pkMember) {
         checkMember(pkMember ? pkMember->GetEntityHandle() : entt::null); };

   Flipping CParty's iterators themselves would reach dungeon.cpp,
   CombatSystem, ItemSystem_LegacyBridge, SocialSystem, questlua_* and
   input_main - a separate phase.

Two tooling bugs, caught and reverted
-------------------------------------
The first attempt at this pass was reverted whole rather than patched:

- The lambda-invocation fixer rewrote the string in place while iterating a
  match list computed on the *old* string, so every match after the first used
  a stale offset. It produced `applyMember(m)er(pkM);` - a syntax error, so the
  compiler caught it, but the same bug in a whole-word-safe rewrite could have
  landed silently. The fixer now builds the output in one pass.
- The CParty adapter was inserted at the call site, which is often a braceless
  `if (party) ...` branch, turning one statement into two and detaching the
  `else`. Adapters are now declared right after the lambda they wrap, which is
  always at statement scope.

Dungeon group: 351 -> 196 conversion sites (819 -> 196 across both passes)
Project-wide conversion count: 3909 -> 3730
Build: RelWithDebInfo GameServer, 0 errors.

What is left in this group (196 sites)
--------------------------------------
Almost all of them are on spawned NPCs and mobs, not on players: clone (36),
target (20), source (11), boss, stone, gate, metin, statue, totem, memorial.
These come from CHARACTER_MANAGER::SpawnMob, which still returns LPCHARACTER -
the same identity boundary that phase 18c deliberately left alone. They cannot
move until the spawn/lookup API returns entities.
LostCastleDungeon keeps 76 of the 196 because it is the clone-heavy one.


Phase 18h - identity boundary: entity-returning lookups
--------------------------------------------------------
AUDIT FIRST. Before touching anything, every site that assigns from a
CHARACTER_MANAGER lookup or factory was measured:

  api                     sites   conversions   remaining pointer uses
  FindByPID                  40           222                      495
  Find                       40           170                      237
  CreateCharacter             5            51                      135
  FindPC                     30            44                       68
  SpawnMob                   30            33                       73
  (+ SpawnMobRange, SpawnMobRandomPosition, FindSpecifyPC)
  TOTAL                     150           531                     1035

The conclusion matters more than the numbers: flipping the whole lookup API's
return type would remove 531 conversions but leave 1035 real legacy CHARACTER
method calls behind, each needing a LegacyCharOf alias. It would relocate work
rather than remove it, and it would make ~118 sites temporarily *worse*
(entity + immediate alias + split null handling).

So the identity boundary is being opened where it is genuinely free, not
big-bang. What actually blocks those 1035 uses is the legacy method surface,
not the lookup API.

What was added
--------------
char_manager.h/.cpp gained three entity-returning counterparts:

  entt::entity FindEntity(uint32_t dwVID);
  entt::entity FindPCEntity(const char* name);
  entt::entity FindEntityByPID(uint32_t dwPID);

FindEntity goes straight to CVIDRegistry - which is already the ECS-side
authority for VID -> entity, and which Find() already consults first, logging
VID_DRIFT when it has to fall back. The other two convert once inside the
manager. No new bookkeeping, no second source of truth, no drift risk.

What was migrated
-----------------
33 sites in 12 files where the looked-up local is *only* ever fed to
conversions and null tests. At those sites the pointer disappears completely:

  -  LPCHARACTER pkChr = CHARACTER_MANAGER::instance().FindByPID(info->dwPID);
  -  if (pkChr == nullptr) return 0;
  -  ... GetMapIndex(((pkChr) ? (pkChr)->GetEntityHandle() : entt::null)) ...
  +  const entt::entity pkChr = CHARACTER_MANAGER::instance().FindEntityByPID(info->dwPID);
  +  if (pkChr == entt::null) return 0;
  +  ... GetMapIndex(pkChr) ...

68 conversions removed, plus 20 boolean uses made explicit (entt::entity has
no conversion to bool, so `if (v)` had to become `if (v != entt::null)`).

Three tooling failures on the way, all reverted whole rather than patched
------------------------------------------------------------------------
This pass was rewritten from scratch three times. Worth recording, because the
first two mistakes are the kind that can land silently:

1. Multiple edits inside one function body: the driver spliced the file per
   edit while other recorded edits still pointed into the old text. Now one
   edit per body per pass, and the driver re-runs until a pass is empty.

2. The "is this local only used through conversions?" test accepted any use
   preceded by "(", so `if (pkVictim->GetArena())` and
   `make_pair(key, ch)` counted as null tests. It is now a strict whitelist of
   exact null/truth-test shapes; anything else disqualifies the site.

3. The conversion regex allowed the parentheses to be optional on both sides,
   so against the single-paren spelling it swallowed the *enclosing call's*
   parenthesis:
       GetPacketVID(v ? v->GetEntityHandle() : entt::null)  ->  GetPacketVIDv
   It now matches three explicit shapes and never consumes a paren that
   belongs to a caller. Earlier phases were checked and are unaffected - their
   helpers only ever matched the fully-parenthesised double form.

Project-wide conversion count: 3730 -> 3665
Build: RelWithDebInfo GameServer, 0 errors.

Next, in the order the audit suggests
-------------------------------------
Do NOT flip the lookup return types wholesale yet. The 1035 pointer uses are
legacy CHARACTER methods; migrate that surface first (new_offlineshop_manager
127, party 113, item_manager 80, ...). Every method that moves shrinks the
pointer count at the lookup sites, and the identity flip gets cheaper each
time. Re-run the audit to decide when it has tipped.


Phase 18i - party and offlineshop manager
------------------------------------------
Note on scope: new_offlineshop_manager.cpp is part of the OLD offline shop,
which SRC/Server/OfflineShop/ is being written to replace, and the recorded
rollout plan has these files deleted once that integration lands. That was
raised before starting; the user chose to migrate them anyway, so the old
surface stays uniform with the rest of the tree for as long as it runs.

party
-----
party.h: 13 declarations flipped.
party.cpp: 13 methods, 75 conversion sites removed.

  ComputeRolePoint 30, Link 10, CreateParty 8, SendPartyInfoOneToAll 4,
  SendPartyInfoAllToOne 4, Unlink 4, SendPartyLinkOneToAll 3,
  SendPartyUnlinkOneToAll 3, SendPartyLinkAllToOne 2, SendPartyJoinAllToOne 2,
  IsPositionNearLeader 2, SendMessage 2, SendParameter 1.

  Six of the Send* methods had zero legacy pointer uses - pure flips.
  14 external call sites adapted (char_manager, AISystem, CombatSystem,
  PlayerRuntimeSystem, PointSystem, SessionSystem, SocialSystem) plus 21
  intra-file calls.

  Checked for std::bind over CParty before starting - none, unlike CGuild in
  phase 18e. The CParty::ForEach*Member templates still call back with an
  LPCHARACTER and were deliberately left alone; the dungeon adapters from
  phase 18g depend on that and are unaffected.

  party.cpp: 114 -> 49. What is left sits in SummonToLeader / HealParty /
  Update / P2PJoin on `l` and `ch` locals pulled out of m_memberMap, whose
  value type is a struct holding LPCHARACTER pCharacter - the same
  container-boundary category as guild's m_memberOnline.

offlineshop
-----------
new_offlineshop_manager.h: 42 declarations flipped.
new_offlineshop_manager.cpp: 42 methods (the whole Recv*/Send*/Encode* client
packet surface), ~100 conversion sites removed.

  28 call sites in input_main.cpp's OfflineshopPacket* handlers, 9 in the
  sibling files (new_offlineshop.cpp, new_offlineshop_safebox.cpp), 15
  intra-file calls, and SessionSystem's RemoveGuestFromShops.

  One definition was missed by the first sweep because it carries a trailing
  comment between the parameter list and the brace:

      bool CShopManager::RecvOfferListRequestPacket(LPCHARACTER ch) //offlineshop-updated 03/08/19
      {

  Same shape as the ACMD trailing-comment case in phase 18; the header had
  already been flipped, so it surfaced as "overloaded member function not
  found" rather than a type error. Worth remembering: a header-first sweep
  turns a missed definition into a confusing diagnostic.

  new_offlineshop_manager.cpp: 133 -> 50. The remainder is on chGuest /
  chBuyer / chOwner / owner locals that come from FindByPID and are still
  passed to legacy CHARACTER methods, plus the guest lists (LISTGUEST is a
  std::list<LPCHARACTER>).

Project-wide conversion count: 3665 -> 3563
Build: RelWithDebInfo GameServer, 0 errors.


Phase 18j - container boundary: CGuild's online-member set
-----------------------------------------------------------
MEASURED FIRST, and the measurement narrowed the job considerably.

"The container boundary" sounded like one large shared problem. It is not.
Counting conversions whose subject is a dereferenced iterator gives 51 sites
project-wide, and 41 of them are in guild.cpp. The LPCHARACTER-holding
containers are spread across many owners (CHARACTER's own m_set_pkChrSpawnedBy
/ m_setExceptChainLighting / m_set_pkChrTargetedBy, CHARACTER_MANAGER's five
sets and maps, CDungeon, CWarMap, wedding, offlineshop's LISTGUEST), and most
of them are iterated to call legacy CHARACTER methods, so flipping their
element type would buy nothing.

CGuild::m_memberOnline is the exception, and it is a clean one: every single
dereference of that container in guild.cpp and guild_war.cpp is
`(*it)->GetEntityHandle()`. 37 of them. The pointer is never used for anything
else - not one legacy method call - so the element type can simply become the
entity.

  guild.h:  typedef CHARACTER_SET TGuildMemberOnlineContainer;
         -> typedef std::unordered_set<entt::entity> TGuildMemberOnlineContainer;

Set membership is unchanged: GetEntityHandle() is a stable per-character value,
insert and erase use the same value, and distinct characters still map to
distinct keys. The one remaining pointer-shaped mutation (RemoveMember, where
only the pointer is in hand) converts at the call.

Also converted, because the element type forced it:
  - 9 loops that pulled the element into a named local (`auto* ch = *it;`).
    Four of those bodies still call a legacy method, so they keep one
    pk-prefixed alias; the rest are entity-only now.
  - two while-loops whose next line already did
    `const entt::entity member = onlineMember ? onlineMember->GetEntityHandle() : ...`
    - the local collapsed into that entity outright.
  - FGuildChat::operator() - pure, no alias needed.
  - FGuildReward::operator() - keeps one alias, it calls ch->SetGold(0).

A mistake worth recording
-------------------------
While clearing the last loops I ran a file-wide substitution of the `ch`
conversion in guild_war.cpp instead of scoping it to the loops I had just
changed. It hit CGuild::GuildChat's own `ch = GetMasterCharacter()`, which is
still a pointer. The compiler caught it immediately (argument 1 cannot convert
from LPCHARACTER to entt::entity) and it was the only casualty, but the lesson
is the same one as phase 18h: scope every rewrite to the block being changed,
never to the file.

guild.cpp: 67 -> 16   guild_war.cpp: 14 -> 1
Project-wide conversion count: 3563 -> 3499
Build: RelWithDebInfo GameServer, 0 errors.

The rest of the container boundary, and why it stays
----------------------------------------------------
CHARACTER_SET is used by CHARACTER itself, CHARACTER_MANAGER (state list,
delayed-save set, monster-log set, race-num map, pending-destroy set), CDungeon,
CWarMap and wedding. Those are iterated to call legacy CHARACTER methods
(UpdateStateMachine, Save, ...), so their element type cannot move before those
methods do. Same for party's m_memberMap, whose value type is a struct holding
LPCHARACTER, and offlineshop's LISTGUEST.


Phase 18k - CombatSystem.cpp: hoisting repeated conversions
------------------------------------------------------------
CombatSystem.cpp holds 105 legacy CHARACTER:: method bodies that were moved
into the ECS system file but never migrated. Their signatures belong to the
legacy class, so flipping them would mean editing char.h - premature. What the
file actually suffers from is repetition: the same local converted over and
over inside one function.

Measured before deciding: 481 conversion sites, of which 436 are repeats of a
subject already converted earlier in the same function. Hoisting each repeated
subject onto one local removes them with NO signature change and no effect
outside this file. That is the whole of this phase.

  Damage(pAttacker)               86 sites -> 1
  Reward()                        pkAttacker 39, ch 16, rch 6, mch 5
  CFuncShoot                      pkVictim 38, m_me 24
  RewardGold(pkAttacker)          26 -> 1
  CalcReferenceNormalHitDamage    pAttacker 17, pVictim 9
  ProcessStoneSpawnStep(ch)       22 -> 1
  GiveExp(from, to) x2            to 19 and 18
  ... 38 subjects in all

Refused on purpose (the guard, not an oversight)
------------------------------------------------
A subject that the body ASSIGNS cannot be hoisted - the hoisted value would be
the one from before the assignment. The tool refuses those and says so:

  m_pkChrTarget  18 sites  (SetTarget assigns the member it converts)
  pkKiller       38 sites  (Dead reassigns it)
  ch             16 sites
  pAttacker       6 sites
  pkChr           2 sites

Those 80 sites stay. They need the surrounding method migrated, not a hoist.

Three tool bugs, each reverted whole and fixed
----------------------------------------------
This pass was rewritten three times. All three were mechanical, all three were
caught by the compiler, and two of them are repeats of mistakes recorded
earlier in this document - which is why the fixes are now shared code:

1. The loose conversion regex again (phase 18h). Matching with optional parens
   swallowed the enclosing call's parenthesis and produced things like
   `ecs::PlayerRuntime::IsPCattacker)`. The three safe patterns now live in
   scratchpad/convmatch.py and every tool uses them.

2. Stale offsets again (phases 18g, 18h). Several subjects can be hoisted
   inside one function; editing the body once per subject left every later
   plan pointing into text that had already moved. All edits for one body are
   now computed on the original text and applied in a single ordered pass,
   with an assertion that they never overlap.

3. New one: a `namespace { }` block was treated as a function body, so
   declarations naming a constructor parameter were hoisted to namespace
   scope. A function body's `{` is preceded by the parameter list's `)`; the
   tool now requires that, which also excludes struct, class and enum blocks.

Also: three declarations landed at column 0 because the line following the
opening brace was a preprocessor directive, which carries no indentation.
Cosmetic, fixed by hand.

CombatSystem.cpp: 449 -> 150
Project-wide conversion count: 3499 -> 3130
Build: RelWithDebInfo GameServer, 0 errors.


Phase 18l - tree-wide hoist sweep
----------------------------------
The hoist tool from phase 18k, once hardened, applies to the whole tree. It
was run in report mode over every remaining .cpp first, then applied.

  71 files changed, 1190 conversion sites removed, no signature changes.

  New_PetSystem 92, arena 90, exchange 66, LostCastleDungeon 63,
  ItemSystem_LegacyBridge 54, guild_renewal 52, cmd_general 43, item_manager 38,
  input_main 37, MountSystem 34, trigger 32, war_map 30, shop_manager 29,
  log 28, shop 26, questlua_global 26, cmd_gm 25, party 22, attr_transfer 22,
  fishing 21, dungeon 17, PetSystem 16, ... and 49 smaller files.

Four new failure modes, all caught by the compiler
--------------------------------------------------
A sweep this wide found edge cases a single file never would. Each is now a
guard in the tool, not a note:

1. Keyword collision. `m_pkChar` produced a local named `char`. The name
    generator now appends Entity for any C++ keyword. 111 identifiers had to be
    renamed after the fact (charEntity), verified not to have touched a single
    real `char` type.

2. switch scope. A declaration inserted at the top of a switch body sits before
    the first `case`, so its initialization is "skipped by case label".
    Two sites (ItemSystem_LegacyBridge, New_PetSystem) - moved above the switch.

3. if-init statement. `if (const auto ch = ...; cond)` - the tool found the
    init-statement's `;` and inserted there, splitting the condition in half.
    Three sites in questlua.cpp - rewritten as two plain statements plus the if.

4. Name collision with a pre-existing local. trigger.cpp already had
    `const entt::entity chr` built from a different subject in the same body;
    the hoist of m_pkChr wanted the same name. Renamed to ownerChr.

Plus one non-code failure: several builds stopped mid-flight during the earlier
phases left orphaned cl.exe processes holding the PDB, which surfaced as
"cannot open program database ... vc145.pdb". Not a code problem - the
stragglers had to be cleared before the build could run.

Project-wide conversion count: 3130 -> 1940
That is 6354 -> 1940 since the OOP->ECS elimination began: 69% removed.
Build: RelWithDebInfo GameServer, 0 errors.

Where the remaining 1940 sit
----------------------------
  ItemSystem_LegacyBridge  252   legacy CHARACTER/ITEM method bodies
  cmd_gm                   216   tch / victim subjects (only `ch` was flipped)
  CombatSystem             150   the 80 refused (reassigned) + singles
  input_login               87
  party                     58   m_memberMap's struct value type
  input_main / item_manager / New_PetSystem / offlineshop / char_manager ~50 each

Roughly half are single-use conversions, which a hoist cannot help - they need
the surrounding method migrated. The other half are the LPITEM axis, which has
not been touched at all yet.


Phase 18m - UseItemEx: the one function the hoist had to refuse
----------------------------------------------------------------
After the tree-wide sweep, an audit of LPITEM-parameter functions showed 149
conversions across 36 functions - and 113 of them in a single one:

    bool CHARACTER::UseItemEx(LPITEM item, TItemPos DestCell)   // 5759 lines

The generic hoist refused it, and the refusal was right: the body reassigns
`item` three times (`item = item2;`), so one hoisted value would be stale
afterwards. But three assignments against 113 conversions is worth doing by
hand.

Checked before writing anything:
  * `item` is a by-value LPITEM parameter, not a reference;
  * exactly three assignments, all the plain `item = item2;`;
  * no `&item`, no LPITEM& parameter in the body, no `goto`.

So one MUTABLE entity, refreshed immediately after each assignment, is exactly
equivalent. A redundant inner `entt::entity itemEntity` in a nested block was
folded into it.

The ordering bug this exposed
-----------------------------
The first attempt inserted the three refresh lines and THEN replaced the
conversions. A refresh line is itself spelled

    itemEntity = item ? item->GetEntityHandle() : entt::null;

so the conversion pass matched the refreshes and rewrote them into

    itemEntity = itemEntity;

A no-op. It compiles, links, and silently leaves the entity stale after every
reassignment - the first bug in this whole migration that the compiler would
NOT have caught. It was found by reading the three assignment sites after the
transform rather than trusting the count. The script now does the replacement
first, inserts refreshes second, and asserts that no `itemEntity = itemEntity;`
survives.

Worth generalising: whenever a rewrite inserts text that the same rewrite's
own pattern can match, order matters and an explicit assertion is cheap.

ItemSystem_LegacyBridge.cpp: 252 -> 146
Project-wide conversion count: 1940 -> 1831
Build: RelWithDebInfo GameServer, 0 errors.

Remaining LPITEM work
---------------------
The other 36 item-parameter functions carry one conversion each - single uses
that a hoist cannot improve. Reducing those means flipping LPITEM parameters
to entt::entity the way the character surface was flipped, which is a separate
axis and should get its own audit first (CItemRegistry already provides
Find / FindByVID / FindByLegacy, so the entity side exists).


Phase 18n - LPITEM signature axis: AUDITED AND NOT DONE
--------------------------------------------------------
The previous phase ended by recommending the LPITEM signature axis as "the one
remaining structural gain". The audit says otherwise, so it was not done. The
numbers are recorded here so nobody repeats the analysis.

LPITEM parameters, across the whole tree
----------------------------------------
  pure    15 parameters,  15 conversions   (pointer would disappear)
  alias   20 parameters,  23 conversions   (one alias line each)
  none    54 parameters,   0 conversions   (flipping buys nothing at all)

38 conversions in total. Against them stand the pointer uses those same
parameters carry:

  UseItemEx     item      4 conversions vs 449 pointer uses
  EquipItem     item      1  vs  62
  AutoGiveItem  item      1  vs  36
  ReceiveItem   item      1  vs  19

Flipping these would remove 38 conversion sites and force ~600 legacy CItem
method calls through LegacyItemOf aliases. That is the identity-boundary
conclusion of phase 18h all over again, only with a far worse ratio - and more
than half the LPITEM parameters in the tree carry no conversion at all, so for
them the flip is pure churn.

Phase 18m already took the only part of this axis that paid: UseItemEx's 112
repeats.

Item lookup locals
------------------
The item counterpart of phase 18h's FindEntity work needs no new API -
ItemSystem::FindItemByID / FindItemByVID and CItemRegistry::Find / FindByVID /
FindByLegacy already return entities. But of the item locals assigned from a
lookup, only 2 sites (3 conversions) are conversion-only. The rest are used as
pointers immediately.

Mechanical work is exhausted
----------------------------
The hoist tool was re-run in report mode over every remaining .cpp:
zero hoistable subjects anywhere in the tree.

Classification of the remainder (1080 sites the classifier can attribute):

    405  38%  subject is reassigned in its function - the hoist refuses these
              on purpose; each needs the UseItemEx treatment, examined by hand
    402  37%  repeats the tool declines for scope reasons (subject belongs to a
              nested lambda or functor, or is declared more than once)
    245  23%  a single conversion in its function - nothing to collapse
     28   3%  file scope / macro bodies

None of these are transformation problems any more. Removing them means moving
the surrounding legacy CHARACTER:: and CItem:: methods into the ECS systems -
design work, not rewriting. cmd_gm.cpp is the clearest illustration: its 64
remaining item sites are ~60 copies of

    item = ITEM_MANAGER::instance().CreateItem(<vnum>);
    if (!item || !item->EquipTo(ch, item->FindEquipCell(ch)))
        ItemSystem::DestroyItemEntityEcs((item ? item->GetEntityHandle() : entt::null), ...);

one conversion per distinct item, with the item reassigned between them. There
is nothing to hoist and nothing to flip; the block itself has to go.

Where the elimination ended
---------------------------
  6354 -> 1831 conversion sites, 71% removed, across 15 phases.
  Every phase built clean (RelWithDebInfo GameServer, 0 errors).


Phase 19 - LPCHARACTER -> entity on the CHARACTER method surface
=================================================================
This is the axis that was deferred through every earlier phase as "premature",
now taken deliberately. The audit is unfavourable and was reported as such
before starting; the direction was chosen anyway, so it is being done in
clusters that each build and commit on their own.

Audit (54 CHARACTER:: definitions taking LPCHARACTER parameters):
    82 conversions would go
   165 pointer uses would stay, behind one alias per body
   239 call sites for those method names

The shape of the change
-----------------------
The method STAYS on CHARACTER. Its body is `this`-relative - member access is
implicit everywhere - so it cannot be moved into an ECS system mechanically.
Only the parameter becomes an entity, with one pk-prefixed alias where the
body still needs the pointer. Exactly the playbook of phases 3-7, applied to
char.h at last.

The payoff that makes it worthwhile is not the conversion count. It is that
the ECS-side wrappers stop converting:

  -    legacyVictim->Dead(LegacyCharOf(killer), immediate);
  +    legacyVictim->Dead(killer, immediate);

Seven such wrappers in CombatSystem.cpp are now genuinely thin. That is the
direction of the whole migration: the entity flows through, and the pointer is
resolved once, at the boundary.

Phase 19a - combat cluster
--------------------------
14 methods flipped: Dead (38 conversions on its own), Attack, Follow,
RewardGold, DistributeSP, DistributeHP, RegisterDamageForExp,
GetNearestVictim, SendDamagePacket, BeginFight, AttackedByPoison,
AttackedByBleeding, AttackedByFire, ItemDropPenalty.
52 conversions removed, 14 declarations in char.h, ~50 call sites adapted.

What the mechanical pass could NOT be trusted with
--------------------------------------------------
A name-based call-site rewrite is unsafe here, and the report mode showed why
before anything was written: arena.cpp has its own CArena::Attack and
CArena::Dead with completely different parameters, dungeon.cpp has
CDungeon::Dead, and the pattern also matched the definitions themselves. The
script was narrowed to receiver-qualified member calls with no type keywords
in the argument list, and everything else was left to the compiler to point
at - four rounds of it.

Two further things the compiler found:
  * `ENTITYISH` guessing. The script assumed arguments named target/victim/
    killer were already entities. In LostCastleDungeon and AISystem they were
    pointers. Guessing a type from a name does not work; those five sites were
    fixed individually.
  * Six `const entt::entity X = X;` lines - the entity locals hoisted in
    phases 18k/18l, now superseded by the parameter itself. Deleted.

Project-wide conversion count: 1831 -> 1823
Build: RelWithDebInfo GameServer, 0 errors.

The count barely moves, and that is expected: 52 conversions left the method
bodies but most call sites had to take one on instead. The gain is structural,
not numeric - the char.h combat surface now speaks entities.
